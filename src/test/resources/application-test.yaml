spring:
  kafka:
    bootstrap-servers: none
  liquibase:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework: INFO


secret:
  key: test-secret-for-tests-test-secret-for-tests